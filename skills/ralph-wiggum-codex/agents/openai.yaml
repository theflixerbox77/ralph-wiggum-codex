interface:
  display_name: "Ralph Wiggum Codex"
  short_description: "Objective-first work/review loop for long-running Codex tasks"
  default_prompt: "Use $ralph-wiggum-codex to keep iterating on a coding task until the task is complete or genuinely blocked, using acceptance criteria, fresh-context review, and optional verification as evidence."

policy:
  allow_implicit_invocation: false
