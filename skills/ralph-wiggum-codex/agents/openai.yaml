interface:
  display_name: "Ralph Wiggum Codex"
  short_description: "Long-running autonomous refinement loop for Codex tasks"
  default_prompt: "Use $ralph-wiggum-codex to run an iterative refinement loop with objective/feedback files, validation checks, and explicit completion criteria."

policy:
  allow_implicit_invocation: false
