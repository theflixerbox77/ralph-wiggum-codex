interface:
  display_name: "Ralph Wiggum Codex"
  short_description: "Long-running fix-and-validate loop for complex Codex tasks"
  default_prompt: "Use $ralph-wiggum-codex to keep iterating on a coding task until the validations pass or the work is clearly blocked."

policy:
  allow_implicit_invocation: false
